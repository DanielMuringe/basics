def first(i):
    for c in i:
        eval(f"from {c[0]} import {c[1]}")

s = 

def recurse(recursable):

    main_list = []
    main_var = ''

    def main(many):
    is_list  = lambda x: isinstance(x, list)
    is_dict  = lambda x: isinstance(x, dict)
    is_tuple = lambda x: isinstance(x, tuple)
        is_many  = lambda y: is_list(y) and is_tuple(y) and is_dict(y)

        if is_many(many):
            index = many
            main_var.append(index)
        
        
        
    
